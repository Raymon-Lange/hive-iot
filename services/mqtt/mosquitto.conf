# Prototype config: plaintext, no auth. NOT for production —
# Epic 1 (Device Identity) will add TLS + client cert auth later.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
